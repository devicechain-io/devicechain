{
  "belowPageCutNote": "This tier’s default, below the provider-list page cut",
  "clearConfirmLabel": "Clear",
  "clearDefaultConfirmDescription": "Clearing the default leaves {{tier}} with no default — no other model is promoted in its place, so every tenant at {{tier}} that has not chosen a model itself will resolve to no model.",
  "colDefault": "Default",
  "colGrant": "Grant",
  "colProvider": "Provider",
  "confirmStrandingTitle": "Leave {{tier}} with no default model",
  "defaultDisabledWarningNoTenants": "No tenants are packaged here yet, but once one is, it will get no model for any AI function they have not assigned themselves — this tier's default ({{provider}}) is disabled, and nothing takes the call in its place.",
  "defaultDisabledWarningWithTenants_one": "{{count}} tenant packaged here gets no model for any AI function they have not assigned themselves — this tier's default ({{provider}}) is disabled, and nothing takes the call in its place.",
  "defaultDisabledWarningWithTenants_other": "{{count}} tenants packaged here get no model for any AI function they have not assigned themselves — this tier's default ({{provider}}) is disabled, and nothing takes the call in its place.",
  "defaultModelAriaLabel": "Default model for {{tier}}",
  "defaultSetToast": "{{tier}} defaults to “{{value}}”",
  "description": "Which AI models each tier offers, and which one it falls back to. Granting a model puts it on the menu of every tenant at that tier; the default is what those tenants get for a function they have not assigned a model to themselves. They are separate decisions — a tier can offer models and default to none, in which case its tenants must each choose.",
  "grantAriaLabel": "Grant {{provider}} to {{tier}}",
  "grantFirstTitle": "Grant this model first",
  "grantedToast": "“{{provider}}” is on the {{tier}} menu",
  "loadingPackaging": "Loading packaging…",
  "makeDefaultAriaLabel": "Make {{provider}} the default for {{tier}}",
  "noDefaultHint": "Tenants at this tier must each choose a model",
  "noDefaultLabel": "No default",
  "noDefaultToast": "{{tier}} has no default model",
  "noDefaultWarningNoTenants": "No tenants are packaged here yet, but once one is, it will get no model for any AI function they have not assigned themselves — this tier offers models but marks none of them its default.",
  "noDefaultWarningWithTenants_one": "{{count}} tenant packaged here gets no model for any AI function they have not assigned themselves — this tier offers models but marks none of them its default.",
  "noDefaultWarningWithTenants_other": "{{count}} tenants packaged here get no model for any AI function they have not assigned themselves — this tier offers models but marks none of them its default.",
  "noProvidersRegistered": "No AI providers registered yet. Register one before packaging it.",
  "noTiersDefined": "No tiers defined yet. Tiers are what models are packaged onto.",
  "revokeConfirmLabel": "Revoke",
  "revokeDefaultConfirmDescription": "“{{provider}}” is {{tier}}’s default. Revoking it leaves {{tier}} with no default — no other model is promoted in its place, so every tenant at {{tier}} that has not chosen a model itself will resolve to no model.",
  "revokedToast": "“{{provider}}” is off the {{tier}} menu",
  "tenantCountBadge_one": "{{count}} tenant",
  "tenantCountBadge_other": "{{count}} tenants",
  "tierHasNoDefaultAriaLabel": "{{tier}} has no default model",
  "title": "AI packaging",
  "truncatedNotice": "Showing the first {{count}} of {{total}} providers. The panels below are not complete — a tier may grant a provider that is not listed here.",
  "unknownTierBadge": "unknown tier",
  "unknownTierExplanation": "No tier with this token exists, so nothing resolves through these grants — no tenant can report a tier the catalog does not have. It is shown because this is the only screen that can reveal it: ai-inference cannot check a tier token when a grant is written, since the catalog lives on a plane its credential cannot reach. Untick every model below to clear it."
}
