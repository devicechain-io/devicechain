<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <!-- Tests exercise reflection-based test fixtures; the AOT guarantee is on the library. -->
    <IsAotCompatible>false</IsAotCompatible>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
    <!-- Lets the real-broker tests SKIP visibly when no broker is present, rather than passing
         vacuously. A skip that reads as a pass is how "no findings" comes to mean "never ran";
         MqttRealBrokerGuardTests is what stops that happening in CI. -->
    <PackageReference Include="Xunit.SkippableFact" Version="1.4.13" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/DeviceChain.Sdk/DeviceChain.Sdk.csproj" />
  </ItemGroup>

  <!-- A minimal Kestrel host stands up a loopback WebSocket server so the graphql-transport-ws
       client is exercised end-to-end (handshake + init/ack + next/complete). -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
