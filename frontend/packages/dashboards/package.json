{
  "name": "@devicechain/dashboards",
  "version": "0.0.1",
  "description": "DeviceChain dashboard runtime — the DashboardHub (subscription multiplexing over the client wire) and the dashboard-definition/selector types (ADR-039).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@devicechain/client": "*",
    "@graphql-typed-document-node/core": "^3.2.0"
  },
  "peerDependencies": {
    "graphql": "^16.0.0"
  },
  "devDependencies": {
    "graphql": "^17.0.2",
    "typescript": "~5.6.0",
    "vitest": "^2.1.9"
  }
}
