{
  "_comment": [
    "USER-FACING highlights for the release named by `version`, published to the website as",
    "part of https://devicechain.io/releases.json (see hack/build-release-manifest.sh).",
    "",
    "🔴 UPDATE THIS IN THE PR THAT PREPARES A RELEASE, BEFORE TAGGING. The `guard` job fails",
    "the release when `version` does not match the tag, deliberately and as the FIRST job —",
    "so a forgotten update costs nothing but a re-run, rather than publishing last release's",
    "highlights against this release's version. Confidently wrong is worse than absent, and",
    "this file is the one part of the manifest a machine cannot derive.",
    "",
    "WRITE FOR SOMEONE DECIDING WHETHER TO UPGRADE, not for someone reading the commit log.",
    "The changelog is already generated from commit subjects and is right there in the release",
    "notes; repeating `fix(events): give payload rows and anchors identities of their own` here",
    "adds nothing. Say what changed for the person running the platform.",
    "",
    "Keep it to a handful of lines. This renders in a download menu and a docs banner, not in",
    "a document — a fifteen-item list is a changelog wearing a different hat.",
    "",
    "`breaking` drives an automatic warning on the website and in the docs. Set it true when an",
    "upgrade is not a plain `helm upgrade`, and say so in the first highlight."
  ],
  "version": "v0.12.0",
  "breaking": true,
  "highlights": [
    "This upgrade is not a plain helm upgrade \u2014 read the upgrade notes first. Devices must move to the per-device command-response topic, two configuration keys moved between areas, and a reading is now stored at its own sample time rather than the moment it arrived.",
    "Geofences: a detection rule can ask whether a device is inside a named fence, answered against the fences that were live when the event happened, so a replay agrees with the original.",
    "Fleet commands: send one command to a device group, watch it land device by device, and cancel it mid-flight.",
    "A command to an absent device is now held until the device returns instead of being published into the void.",
    "A presence source that stops reporting hands its devices back instead of leaving them frozen offline.",
    "Buffered uploads keep their own timestamps, so a device that stores and forwards no longer lands an hour of history at a single instant."
  ]
}
