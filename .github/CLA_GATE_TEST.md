# CLA gate test (throwaway)

This file exists only to open a test PR that triggers the CLA Assistant Lite
workflow (`.github/workflows/cla.yml`). Do not merge — close the PR and delete
the branch once the signature flow is verified.
