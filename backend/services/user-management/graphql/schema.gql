# Represents a user assigned to a tenant.
type TenantUser implements Model {
    id: ID!
    created_at: String
    updated_at: String
    deleted_at: String
    firstName: String!
    lastName: String!
    email: String
    username: String!
    enabled: Boolean
}

# Contains queries executed against model.
type Query {
}

# Contains mutations executed against model.
type Mutation {
}

schema {
    query: Query
    mutation: Mutation
}
