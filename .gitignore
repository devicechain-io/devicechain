# Agent OS — strategy/instructions not shared publicly
.agent-os
.claude/commands/
.claude/agents/
.claude/skills/
# Agent worktrees are created INSIDE the repo, so without this they show up as
# untracked and are one `git add -A` away from being committed -- which has
# happened here before, at 428 files. They also make the working tree
# permanently dirty, which matters more than tidiness now that the release job
# refuses to run goreleaser from a dirty checkout.
.claude/worktrees/
.claude/settings.json

# Legacy individual repos (reference only, not part of monorepo)
_legacy/

# Go
*.exe
*.test
*.out
vendor/
dist/
# compiled simulator binary (extensionless, produced by `go build` in the sim dir)
/backend/sims/dc-simulator/dc-simulator

# Frontend
node_modules/
frontend/dist/
frontend/.env
frontend/.env.local
frontend/.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
bin/
build/

# Secrets / local config
*.env
.env
.env.local
config.local.yaml

# OpenTofu / Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
# keep example tfvars; ignore real ones (may hold credentials)
*.tfvars
*.tfvars.json
!*.tfvars.example

# Build artifacts left by "go build ./backend/tools/..." from the repo root.
dc-migrationdiff
dc-drdrill
dc-apiprobe

# Maintainer scratch space. Untracked by convention since it was created; this makes that
# explicit, because convention alone does not stop `git add -A`. It did not: one `git add -A
# backend scratchpad` swept 428 files of working notes, mutation harnesses and drill output
# into a public PR, where CI caught it only because the shellcheck job lints "every tracked
# script" and suddenly had eighty more.
#
# Nothing here has ever been tracked, so ignoring it orphans nothing. Note that CLAUDE.md and
# the arc handoffs cite paths under this directory (scratchpad/HANDOFF-*.md, build specs) —
# those references are to files on a maintainer's disk, not to repo content, and that is the
# arrangement being made explicit rather than changed.
scratchpad/
